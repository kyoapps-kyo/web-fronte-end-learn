import "core-js/stable";
import "../assets/style.css";
let name = "TOM";

const age = 19;

const add = (x, y) => x + y;

new Promise((resolve, reject) => {
  resolve("success");
});

Array.from([1, 2]);

class Person {}
